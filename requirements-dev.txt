# Dev/CI-only tooling on top of the runtime dependencies each task's own
# README installs (see task-8/README.md). Not needed to run the API or
# training pipelines, only to lint and test them.
ruff==0.14.4
pytest==8.4.2
pytest-cov==7.0.0
httpx==0.28.1
pre-commit==4.4.0
